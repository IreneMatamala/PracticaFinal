groups:
- name: example-alerts
  rules:
  - alert: HighCpu
    expr: sum(rate(container_cpu_usage_seconds_total[1m])) by (pod) > 0.8
    for: 2m
    labels:
      severity: warning
    annotations:
      summary: "CPU usage high"
